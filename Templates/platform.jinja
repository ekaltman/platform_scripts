<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />

  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}" />
</head>

<body>

<div id="content">
  <p>{{ title }}</p>

  <ul>
  {% for item in platformInfo %}
    <li>{{ item }}</li>
  {% endfor %}
  <nav><a href="main.html">Home</a></nav>
  </ul>
</div>

</body>
</html>
