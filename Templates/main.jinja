<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />

  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}" />
</head>

<body>

<div id="content">
  <p>{{ title }}</p>

  <ul>
  {% for item in platformInfo %}
    
    <nav><a href={{ item }}>{{ platformNames[loop.index0] }}</a></nav>
  {% endfor %}
  </ul>
</div>

</body>
</html>
